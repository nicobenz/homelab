services:
  docsight:
    image: ghcr.io/itsdnns/docsight:v2026-03-26.1
    container_name: docsight
    restart: unless-stopped
    ports:
      - "8765:8765"
    volumes:
      - ${ROOT_DIR}:/data
    environment:
      - TZ=Europe/Berlin  # fallback; timezone is configurable via the web UI