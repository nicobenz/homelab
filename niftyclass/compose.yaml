services:
  niftyclass:
    image: ghcr.io/nicobenz/niftyclass:0.1.0
    container_name: niftyclass
    restart: unless-stopped
    ports:
      - "3013:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: "3000"